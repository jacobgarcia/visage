.input-container {
  display: inline-flex;
  flex-direction: column;
  label {
    font-size: 0.8rem;
    font-weight: 600;
    color: #333333;
    margin-bottom: 4px;
  }
  input {
    border: 1px solid black;
    padding: 4px 8px;
    border-radius: 3px;
    display: inline-block;
  }
}
